machine:
  node:
    version: 6.2

dependencies:
  override:
    - npm install --dev
  cache_directories:
    - "node_modules"

test:
  override:
    - "DEBUG=yeoman:generator npm test"
    - "npm run test"
