machine:
  node:
    version: 6.2

dependencies:
  override:
    - npm install --only=dev
  cache_directories:
    - "node_modules"

test:
  override:
    - npm test

general:
  artifacts:
    - ./coverage
